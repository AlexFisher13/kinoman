const initialState = {Title: 'TestFilm', Year: '2018'}

export default function movieState(store=initialState, action) {
    // if (action.type === 'GET_MOVIE') {
    //     return action.payload
    // }
    return store
}